name: ride_app
description: "DriveOn - Intercity Ride Sharing"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.11.3

dependencies:
  flutter_map: ^8.3.0
  latlong2: ^0.9.0
  flutter:
    sdk: flutter

  supabase_flutter: ^2.8.0
  flutter_local_notifications: ^17.0.0

  cupertino_icons: ^1.0.8
  google_fonts: ^6.2.1
  geolocator: ^14.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
